spring:
  application:
    name: rag

  ai:
    ollama:
      base-url: http://localhost:11434

      chat:
        model: llama3

      embedding:
        model: nomic-embed-text

logging:
  level:
    org.springframework.ai: DEBUG